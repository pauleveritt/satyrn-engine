# Development record

Every feature cycle in this project produces a design spec and an
implementation plan, both committed rather than thrown away, so you can
read why the code looks the way it does — including the arguments that
were considered and rejected.

The roadmap — phases of feature cycles derived from the two-repo rewrite
brief — and the current phase live in `ROADMAP.md` in this checkout.

```{toctree}
:maxdepth: 1
:caption: Design record
:glob:

specs/*
plans/*
research/*
```
